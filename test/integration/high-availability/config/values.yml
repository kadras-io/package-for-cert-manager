---
apiVersion: v1
kind: Secret
metadata:
  name: cert-manager-values
  namespace: tests
stringData:
  values.yaml: |
    controller:
      replicas: 2
    cainjector:
      replicas: 2
    webhook:
      replicas: 3
